<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.san.spring.dao.BbsDao">
	<insert id="addBbs" parameterType="com.san.spring.dto.BbsDto">
		INSERT INTO MAINBBS
		VALUES(SEQ_MAINBBS.NEXTVAL,#{email},#{title},#{content},#{location},0,#{imgname},#{originfilename},SYSDATE)
	</insert>

	<select id="allBbsList" resultType="com.san.spring.dto.BbsDto">
		SELECT DISTINCT
		M.POST_NUMBER,M.EMAIL, M.TITLE, M.CONTENT,M.LOCATION, M.AUTH_CHECK,
		M.IMGNAME,M.ORIGINFILENAME, M.WDATE,
		B.NICKNAME, COUNT(L.LIKE_COUNT)OVER(PARTITION BY L.MAIN_POST_NUMBER) AS LIKECOUNT
		FROM MAINBBS M , MAINLIKE L , MEMBER B
		WHERE M.POST_NUMBER = L.MAIN_POST_NUMBER AND M.EMAIL = B.EMAIL
		ORDER BY M.WDATE DESC
	</select>

	<select id="checkLike"
		parameterType="com.san.spring.dto.BbsLikeDto"
		resultType="com.san.spring.dto.BbsLikeDto">
		SELECT *
		FROM MAINLIKE
		WHERE MAIN_POST_NUMBER =
		#{main_post_number} AND EMAIL = #{email}
	</select>

	<update id="updateLike"
		parameterType="com.san.spring.dto.BbsLikeDto">
		UPDATE MAINLIKE
		<choose>
			<when test="like_count == 1">
				SET LIKE_COUNT = 0
			</when>
			<otherwise>
				SET LIKE_COUNT = 1
			</otherwise>
		</choose>
		WHERE MAIN_POST_NUMBER = #{main_post_number}

	</update>

	<insert id="addLike"
		parameterType="com.san.spring.dto.BbsLikeDto">
		INSERT INTO MAINLIKE(LIKE_NUMBER, EMAIL,
		MAIN_POST_NUMBER, LIKE_COUNT)
		VALUES(SEQ_LIKE.NEXTVAL, #{email},
		#{main_post_number}, 1)
	</insert>

	<select id="getLikeCount" parameterType="java.lang.Integer"
		resultType="java.lang.Integer">
		SELECT COUNT(LIKE_COUNT)
		FORM MAINLIKE
		WHERE MAIN_POST_NUMBER
		= #{main_post_number}
	</select>

</mapper>  

